name: simple_note_app_fe
description: A simple note taking app using Flutter BLoC architecture
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  http: ^1.1.0
  meta: ^1.14.0
  # 🚫 HAPUS equatable, google_fonts, cupertino_icons (sementara)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # 🚫 HAPUS assets yang tidak ada