name: simple_note_app_fe
description: A simple note taking app using Flutter BLoC architecture
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # Networking
  http: ^1.1.0

  # UI & Icons
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0

  # 🆕 ADD THIS LINE
  meta: ^1.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
